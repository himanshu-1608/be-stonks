# sessions / tokens — never commit
data/*/session.json

# runtime run reports
runs/

# generated dedup log
data/alerts/mapping.csv

# go build artifacts
/bin/
*.test
*.out

# env
.env

# daily input (personal data) — keep out of git; sample is tracked separately
data/alerts/recommendation.csv
